{
    "name": "psoda/wordpress-analysis",
    "description": "Fetch WordPress content via the authenticated REST API, analyse it, and produce a structured report.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.2",
        "ext-curl": "*",
        "ext-json": "*"
    },
    "_comment": "Run `composer require --dev phpunit/phpunit` when you start writing real tests.",
    "require-dev": {},
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
